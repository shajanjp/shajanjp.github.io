---
layout: layouts/base.njk
title: Blog Tags
eleventyExcludeFromCollections: true
permalink: /blog/tag/
---

<section>
  <h2 class="text-2xl font-medium mb-8">All Blog Tags</h2>
  <div class="flex flex-wrap gap-3">
    {% for tag, posts in collections %}
      {% if tag not in ["all", "blog", "posts", "photos", "projects"] %}
        {% set blogCount = 0 %}
        {% for post in posts %}
          {% if "blog" in post.data.tags %}
            {% set blogCount = blogCount + 1 %}
          {% endif %}
        {% endfor %}

        {% if blogCount > 0 %}
          <a href="/blog/tag/{{ tag }}/" class="px-3 py-1 border border-zinc-200 dark:border-zinc-800 rounded-md hover:border-zinc-400 dark:hover:border-zinc-600 transition-colors">
            <span class="text-sm font-medium">{{ tag }}</span>
            <span class="text-xs text-zinc-500 ml-1">({{ blogCount }})</span>
          </a>
        {% endif %}
      {% endif %}
    {% endfor %}
  </div>
</section>
